#!/bin/bash

function task_init()
{
    task "database:available"
    task "assets:apply"

    cd {{ @('backend.path') }}

    {% for step in @('backend.init.steps') -%}
    {{ step|raw }}
    {% endfor %}

    task "welcome"
}
