apiVersion: v1
kind: Service
metadata:
  labels:
    app.service: {{ .Values.prefix }}keycloak
  name: {{ .Values.prefix }}keycloak
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app.service: {{ .Values.prefix }}keycloak
status:
  loadBalancer: {}