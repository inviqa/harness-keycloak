app:
  services:
    keycloak:
      environment:
        KEYCLOAK_HOSTNAME: {{ @('pipeline.qa.hostname') }}
    console:
      environment:
        APP_HOST: {{ @('pipeline.qa.hostname') }}
        KEYCLOAK_EXTERNAL_URL: = 'https://' ~ @('pipeline.qa.hostname') ~ '/auth' 
